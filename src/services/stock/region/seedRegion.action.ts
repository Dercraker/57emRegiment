"use server"

import { superAdminAction } from "@/lib/actions/safeAction"

export const CreateRegionAction = superAdminAction.action(
  async ({ parsedInput }) => {
    try {
    } catch (error) {}
  }
)
