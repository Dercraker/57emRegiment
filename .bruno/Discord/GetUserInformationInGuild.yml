info:
  name: GetUserInformationInGuild
  type: http
  seq: 1

http:
  method: GET
  url: "{{process.env.DISCORD_API_BASE}}/users/@me/guilds/{{process.env.DISCORD_SERVER_ID}}/member"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
