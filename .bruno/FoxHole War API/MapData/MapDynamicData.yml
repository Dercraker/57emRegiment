info:
  name: MapDynamicData
  type: http
  seq: 4

http:
  method: GET
  url: "{{process.env.WAR_API_BASE}}/maps/{{MapName}}/dynamic/public"
  auth: inherit

runtime:
  variables:
    - name: MapName
      value: ClansheadValleyHex

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
