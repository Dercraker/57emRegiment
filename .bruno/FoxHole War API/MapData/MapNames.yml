info:
  name: MapNames
  type: http
  seq: 2

http:
  method: GET
  url: "{{process.env.WAR_API_BASE}}/maps"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
